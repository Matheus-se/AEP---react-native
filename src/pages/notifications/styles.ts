import {StyleSheet} from 'react-native';

export const style = StyleSheet.create({
  paddingVertical: {
    paddingVertical: 20,
  },

  headline: {
    textAlign: 'center',
    marginTop: 20,
    fontSize: 18,
    lineHeight: 21,
  },
});

import { StyleSheet } from 'react-native';

export const style = StyleSheet.create({
    textContainer: {
        marginHorizontal: 20,
    },

    textMessageContent: {
        fontSize: 14,
        lineHeight: 21,
    }
})
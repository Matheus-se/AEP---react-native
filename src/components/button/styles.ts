import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  gradientButton: {
    marginTop: 10,
    borderRadius: 40,
  },
  
  loginText: {
    textAlign: 'center',
    color: 'white',
    fontSize: 15,
    fontWeight: '700',
  },

});
